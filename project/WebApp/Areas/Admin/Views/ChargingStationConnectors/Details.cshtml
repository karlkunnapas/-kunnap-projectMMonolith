@model App.Domain.ChargingStationConnector

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ChargingStationConnector</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ChargingStation)
        </dt>
        <dd class = "col-sm-10">
            @(Model.ChargingStation?.Location ?? string.Empty)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Connector)
        </dt>
        <dd class = "col-sm-10">
            @(Model.Connector?.Id.ToString() ?? string.Empty)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
