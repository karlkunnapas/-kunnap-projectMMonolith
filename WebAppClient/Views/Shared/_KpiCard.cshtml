@model WebApp.ViewModels.Shared.KpiCardViewModel

<div class="kpi-card">
    <div class="kpi-accent @Model.AccentClass"></div>
    <div class="kpi-title">@Model.Title</div>
    <div class="kpi-value">@Model.Value</div>
    <div class="kpi-hint">@Model.Hint</div>
</div>
